# for循环，其中range(1,10)取得1到9之间整数,不会取到10
# range(1,10)相当于数学中的[1,10)，是前闭后开取值范围。
for i in range(1, 10):
    # for循环，取得从1到i的整数
    for j in range(1, i + 1):
        # print()函数默认自带换行，可以添加第二参数end=" "来阻止换行,
        # 并且end=" "可以在每个乘法式子后面加一个空格
        print('{0}*{1}={2}'.format(j, i, i * j), end=" ")
    # 里层for循环完成后加一个换行，通过print('')实现换行
    print('')
