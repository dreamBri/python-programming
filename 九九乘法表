# for循环，其中range(1,10)取得1到9之间的整数,不会取到10
# range(1,10)相当于数学中的[1,10)，是前闭后开取值范围。
对于范围 (1, 10) 内的 i：
    #进行循环，得到从1到i的整数
    对于范围 (1, i + 1) 中的 j：
        # print()默认函数自带换行，可以添加第二个参数end=" "来阻止换行,
        #和end=" "可以在每个乘法子后面加一个空格
        print('{0}*{1}={2}'.format(j, i, i * j), end=" ")
    # 里层为循环完成后加一个换行，通过print('')实现换行
    打印（''）
